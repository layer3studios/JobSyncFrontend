'use client';
// FILE: src/app/(employer)/employer/(app)/(onboarded)/page.tsx
// Employer dashboard index. Auth + onboarding enforced by the layouts; renders
// the ported Dashboard.
import Dashboard from '@/components/employer/Dashboard';

export default function Page() {
  return <Dashboard />;
}

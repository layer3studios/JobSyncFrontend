'use client';
// FILE: src/app/(employer)/employer/(app)/(onboarded)/jobs/new/page.tsx
// Create-posting page. Auth + onboarding enforced by the layouts; renders the
// ported New posting component.
import EmployerJobsNew from '@/components/employer/jobs/New';

export default function Page() {
  return <EmployerJobsNew />;
}
